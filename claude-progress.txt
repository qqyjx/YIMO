# Claude Progress Log — YIMO (Object Extraction & Three-Tier Architecture)
# 追加式日志，每次 session 在末尾追加，不修改历史条目

## 2026-02-16 — Harness Bootstrap
- 项目状态：代码项目，EAV 数据对象提取与三层架构关联系统
- 技术栈：Python, Flask, MySQL, SBERT (语义聚类)
- 核心模块：scripts/（数据处理脚本）+ webapp/（Flask Web 界面）+ mysql-local/（数据库配置）
- 工作流：Excel/CSV 导入 → SBERT 语义去重 → 对象提取 → Web 展示
- Harness 文件已创建：init.sh, task.json, claude-progress.txt
- 下一步：T001 — 环境验证与代码审计

## 2026-02-19 — 项目进展与需求满足度审查
- 审查范围：object_extractor.py(2164行) + olm_api.py(2541行) + 10.0.html(2768行) + bootstrap.sql(598行) + 两域提取结果JSON
- 1.md核心需求满足率：9/9 (100%) — 对象抽取、三层关联、前端展示、项目必须抽取、多域、EAV、SBERT、v10.0风格、旧功能删除
- 0.md愿景需求满足率：6/8 (75%) — Phase 2-6全部实现（生命周期+溯源+机理+预警+治理），2项(HTAP+中台对比)阻塞于甲方输入
- 数据验证：输配电域6对象/11928关联，计财域7对象/1294关联，两域均含OBJ_PROJECT
- 技术指标：42个API端点、24张表+4视图、24个端点支持DB→JSON回退、100%参数化查询无SQL注入
- 主要风险：无认证授权(高)、测试覆盖不足(高)、日志用print(中)、API无分页(中)
- 结论：项目可进入部署验收阶段

## 2026-03-16 — Agent Harness 升级 + doc→docs 重命名

### 完成事项
- **doc/ → docs/ 重命名**：本地已移动，提交并推送到远程。git 自动检测为 rename (100% similarity)
- **CLAUDE.md 路径更新**：7 处 `doc/requirement/` → `docs/requirement-1/`，2 处 `doc/product-design.md` → `docs/product-design.md`，目录树同步更新为 docs/ 实际布局（含新增飞书版文档）
- **task.json 重写**：从 5 个通用占位符任务升级为 11 个真实任务（T001-T050），分 4 个阶段（基础设施/功能完善/质量保障/文档交付），基于需求文档、飞书版架构设计、项目实际状态设计
- **init.sh 增强**：新增 outputs/ JSON 回退数据检查、tests/ 模块检查、pytest 快速收集检查，检查步骤从 6 步扩展到 7 步
- **需求评估（上一轮延续）**：1.md 17/17 全部解决，0.md 6/8 已实现（HTAP + 中台对比阻塞于甲方输入）

### 远程新增文档（git pull 同步）
- docs/系统架构设计-飞书版.md — 系统四层架构设计
- docs/palantir-对标方案-飞书版.md — Palantir Foundry 对标
- docs/团队分工方案-飞书版.md — 三人制分工方案

### 下次优先
- T001：环境健康检查与状态审计（harness 首次正式运行）

---

## 2026-02-27 — 健康检查与仓库清理
- Git同步：本地 main 与 origin/main 完全一致，零差异
- 测试：146 passed, 0 failed, 3 DeprecationWarning（faiss SwigPy 相关，无影响）
- MySQL：端口3307连接正常，19张表，extracted_objects 18行，object_entity_relations 13898行，eav_entities 220883行
- Webapp：Flask 重启后 HTTP 200，148KB 响应正常
- 远程分支清理：删除33个已合并分支（26个 claude/* + 7个 feature/fix/cleanup），保留 main + feature/deploy-progress-bar（未合并）
- 本地分支清理：删除3个 [gone] 本地分支
- 冗余文件清理：删除9个 tracked 文件（4个 TSX 组件 + 1个 TS 服务 + 2个空 shell + 1个 JS + .gitkeep），清理 __pycache__/.pytest_cache/stale PID
- 仓库状态：干净，分支精简，代码健康

## 2026-03-19 — 演示修复 + Agent Harness T001

### 演示修复（commit de178ea）
- 跨域对象合并：4 对重复对象合并（项目/合同→输配电，费用/指标→计财），合并后 16 个对象
- "移交信"→"移交管理"重命名
- 新建 scripts/import_json_to_db.py（JSON→DB 导入+跨域合并工具）
- 创建缺失 DB 表（lifecycle, traceability, mechanism, alert）+ 预置数据
- 添加生命周期演示数据（项目/设备/合同共 9 条记录）
- 归档旧 rules JSON 到 outputs/archive/
- 已推送到 GitHub

### T001 环境健康检查（completed）
- init.sh: Python ✓, 项目结构 ✓, JSON ✓（MySQL 检测方式需优化，实际可连接）
- pytest: 96 passed, 23 errors（PyTorch 2.1.2 + NumPy 2.2.6 版本冲突，非代码问题）
- Webapp: /health 200 OK, FAISS true
- 数据库: 16 对象, 13652 关联, 235336 EAV实体, 9 生命周期, 3 溯源链, 3 机理函数

### 已知障碍
- **OBSTACLE-001**: PyTorch 2.1.2 不兼容 NumPy 2.2.6，导致 SBERT/sentence-transformers 导入失败
  - 影响：test_object_extractor.py 和 test_olm_api.py 无法运行
  - 解决方案：`pip install numpy<2` 或 `pip install torch>=2.2`
  - 不影响 webapp 运行（webapp 在 venv 中有独立依赖）

### 量化指标
- 代码：olm_api.py 3311行, object_extractor.py 2490行, 10.0.html 3304行
- API：44 端点, 100% DB→JSON 回退
- DB：25 张表 + 4 视图
- 测试：96 passed（eav_full 51 + simple_extractor 17 + 部分 olm_api）

### 下次优先
- T020: 对象抽取算法持续优化（如有甲方新反馈）
- OBSTACLE-001: 修复 PyTorch/NumPy 版本冲突
